server:
  port: 80
  baseURL: "/"
  logging:
    - levels: "info|warning|error"
  sources:
    - path: "/srv/shared"
      name: "Shared Folder"
    - path: "/srv/users"
      name: "Personal folder"
      config:
        defaultEnabled: true
        defaultUserScope: "/"
        createUserDir: true
        useLogicalSize: false
auth:
  methods:
    password:
      enabled: true
      minLength: 5
      signup: false

userDefaults:
  disableUpdateNotifications: false
  darkMode: false
  stickySidebar: true
  viewMode: normal
  permissions:
    admin: false
    modify: true
    share: true
    delete: true
    create: true
    download: true
